package studio.eyesthetics.sbdelivery.di.modules

import dagger.Module
import dagger.Provides

@Module
class RepositoryModule {
/*    @Provides
    fun provideEventRepository(
        eventApi: IEventApi
    ) : IEventsRepository = EventsRepository(eventApi)*/
}